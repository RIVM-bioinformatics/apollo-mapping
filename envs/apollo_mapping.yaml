name: apollo_mapping
channels:
  - conda-forge
  - bioconda
  - biocore
  - nodefaults
dependencies:
  - conda-forge::python=3.11.*
  - git=2.40.*
  - conda-forge::mamba=1.3.*
  - bioconda::snakemake=7.24.*
  - conda-forge::pip=23.*
  - pytest=9.0.*      # conda-forge:: ? dev or test dependency ? ci-tools ;-)
  - zstandard=0.19.0  # conda-forge:: ? dev or test dependency ? ci-tools ;-)
  - zstd=1.5.2        # conda-forge:: ? dev or test dependency ? ci-tools ;-)
  #- setuptools=79.0.1
  - pip:
    - -r ../requirements.txt
    # TODO: test if these are automatically taken along in hatch create env
    #- -r ../../juno_library/requirements.txt
    #- -r ../../apollo_mappinng/requirements.txt
    #- -r ../../rivm_idsbioinfo_argparse_utils/requirements.txt